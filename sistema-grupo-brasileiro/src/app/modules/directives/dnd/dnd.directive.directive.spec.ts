import { DndDirectiveDirective } from './dnd.directive.directive';

describe('DndDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DndDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
