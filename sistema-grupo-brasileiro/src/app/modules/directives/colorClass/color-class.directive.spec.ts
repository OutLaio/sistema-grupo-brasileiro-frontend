import { ColorClassDirective } from './color-class.directive';

describe('ColorClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorClassDirective();
    expect(directive).toBeTruthy();
  });
});
